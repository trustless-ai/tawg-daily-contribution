# Acceptance fixture set

The end-to-end acceptance tests deliberately compose the reviewed source fixtures in the sibling `telegram_export.json`, `telegram_updates.json`, `github/`, `magicians/`, and `ai/` directories. No raw production export, Telegram numeric identity, token, or media binary belongs here.

